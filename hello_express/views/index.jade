doctype html
html
	title Pagina con Jade
	body
		- var array = [1,2,3,4,5];
		- for (var i = array.length - 1; i >= 0; i--)
			p.
				Ejemplo de interpolacion de etiquetas. Haga clic en el #[a(href="www.google.com") link] por favor
		
//Ejemplo de interpolacion de etiquetas. SI NO SE PONE PUNTO despues de la P lo toma como etiqueta. (p.)